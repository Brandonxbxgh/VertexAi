# Solana - Required
SOLANA_PRIVATE_KEY=your_base58_private_key_here

# RPC - Helius uses api-key (hyphen), NOT api_key (underscore)
SOLANA_RPC_URL=https://mainnet.helius-rpc.com/?api-key=YOUR_HELIUS_KEY
# Or public: SOLANA_RPC_URL=https://api.mainnet-beta.solana.com

# Jupiter - Required for swap quotes (get free key at https://station.jup.ag/)
JUPITER_API_KEY=your_jupiter_api_key_here

# Arbitrage - Tuned for 0.15-0.2% daily profit target
# Trade size: dynamic from tradable (pool - reserve), or fixed fallback
# TRADE_SIZE_LAMPORTS=10000000   # Fallback if Supabase unavailable
# TRADE_SIZE_PCT=0.005           # 0.5% of tradable
# TRADE_SIZE_MIN_SOL=0.01        # Min 0.01 SOL per trade
# TRADE_SIZE_MAX_SOL=0.5         # Max 0.5 SOL per trade
# MIN_PROFIT_BPS=25              # 0.25% min profit (relaxed for more trades)
# SAFETY_BUFFER_BPS=8            # 0.08% buffer for execution delay
# MAX_PRICE_IMPACT_PCT=0.8       # Reject if any leg > 0.8% impact
# ARB_SLIPPAGE_BPS=60            # 0.6% slippage for more fills
# REQUOTE_BEFORE_EXECUTE=true    # Set false for faster execution (more slippage risk)
# POLL_INTERVAL_MS=3000          # Check every 3 seconds

# Supabase - For trade logging (Phase 3+)
# SUPABASE_URL=https://xxx.supabase.co
# SUPABASE_SERVICE_KEY=your_service_role_key

# Telegram - Optional alerts when trade executes (see TELEGRAM_SETUP.md)
# TELEGRAM_BOT_TOKEN=your_bot_token
# TELEGRAM_CHAT_ID=your_chat_id

# Deposit watcher - For multi-user deposits (requires SUPABASE_URL, SUPABASE_SERVICE_KEY)
# POOL_WALLET=optional - defaults to bot wallet
# DEPOSIT_POLL_INTERVAL_MS=60000   # 60s default, keep high for RPC limits
